# LMS-student_microservice

The Library Management System is a Project handled with the help of two microservices.
The student microservice is the downstream that consumes the upstream service (LMService)
